spring:
  datasource:
    url: jdbc:h2:./target/db/dev.db;AUTO_SERVER=true
    username: user
    password: password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    hibernate.ddl-auto: create